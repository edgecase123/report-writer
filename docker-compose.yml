services:
  report-writer:
    build:
      context: .
    container_name: report-writer
    working_dir: /app
    volumes:
      - ./:/app
      - composer-cache:/root/.composer
    stdin_open: true
    tty: true

volumes:
  composer-cache: