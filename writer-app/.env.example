# Copy to .env for local overrides. .env is gitignored (see repo root .gitignore).
# APP_DEBUG=1 turns on stack traces in 500 responses — dev only, never in prod.
APP_ENV=dev
APP_DEBUG=1
SQLITE_PATH=/app/writer-app/data/report-writer.sqlite
